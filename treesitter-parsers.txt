# astronvim_v5 需要的 treesitter parser 清单
#
# 原来是 251 个（:TSInstall all 的产物，几乎所有语言都装了），
# 减到实际在用的这些。parser 是本地编译的（nvim-treesitter master 分支
# 没有预编译下载机制），所以数量直接决定构建时间。
#
# 加语言怎么办：见 editor/astronvim_v5/lang/ 下的语言包。
c
cpp
python
rust
go
bash
lua
vim
vimdoc
query
markdown
markdown_inline
json
yaml
toml
